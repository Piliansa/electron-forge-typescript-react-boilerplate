import React from "react";
import "./App.scss";
import { hot } from "react-hot-loader";

const App = () => {
    return (
        <main>
            <h1>Hello World!</h1>
        </main>
    );
};

export default hot(module)(App);
